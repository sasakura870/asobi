= content_for(:title, user_name_format(@user))

= content_for(:sidebar)
  = render partial: 'shared/parts/user_profile', locals: { user: @user }

.u-mb3 = render partial: 'users/tab_menu', locals: { user: @user, action: :followings }
= render partial: 'users/user_list_contents', locals: { users: @followings, header_title: 'フォロー中のユーザー', no_contents_message: 'フォロー中のユーザーはいません' }

